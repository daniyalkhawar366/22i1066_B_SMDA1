<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- give the overall size (optional — view size usually controls it) -->
    <size android:width="120dp" android:height="120dp"/>

    <!-- transparent inside so image shows through -->
    <solid android:color="@android:color/transparent" />

    <!-- border -->
    <stroke
        android:width="4dp"
        android:color="#8B5A5A" />
</shape>
