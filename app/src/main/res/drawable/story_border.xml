<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent"/> <!-- keep inside transparent -->
    <stroke
        android:width="2dp"
        android:color="#B0B0B0"/> <!-- light grey border -->
    <size
        android:width="64dp"
        android:height="64dp"/> <!-- ensures circle shape -->
</shape>
